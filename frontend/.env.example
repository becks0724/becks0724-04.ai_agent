# frontend (Vite) 환경변수 템플릿. 실제 값은 .env.local 에만 두고 절대 커밋하지 않는다.
#
# 규칙:
#  - VITE_ 접두사가 붙은 변수만 클라이언트 번들에 포함된다 → 공개 가능한 키만 여기에 둔다.
#  - Supabase의 service_role 키, 거래소 API 키, LLM API 키 등은 worker/.env 에서만 다룬다.

# Supabase 공개(anon) 키 — RLS 정책으로 보호되는 키만 사용한다
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
